---
trigger:
  branches:
    include:
      - main
  paths:
    include:
      - pipelines/build-planner.yml
      - pipelines/templates/steps-build-planner.yml
      - src/NetUpgradePlanner/**
      - src/Terrajobst.ApiCatalog/**
      - src/Terrajobst.NetUpgradePlanner/**
      - src/Terrajobst.NetUpgradePlanner.Excel/**
      - src/Terrajobst.UsageCrawling/**
name: $(date:yyyyMMdd)$(rev:.r)
variables:
  BuildConfiguration: Release
resources:
  pipelines:
    - pipeline: "apis-of-dotnet-build-planner"
      project: "OnlineServices"
      source: 'ASP-IIS-dotnet\builds\apis-of-dotnet-build-planner'
  repositories:
    - repository: 1ESPipelineTemplates
      type: git
      name: 1ESPipelineTemplates/1ESPipelineTemplates
      ref: refs/tags/release

extends:
  template: v1/1ES.Official.PipelineTemplate.yml@1ESPipelineTemplates
  parameters:
    sdl:
      codeql:
        compiled:
          enabled: true
    pool:
      name: AzurePipelines-EO
      image: 1ESPT-Windows2022
      os: windows
    customBuildTags:
      - ES365AIMigrationTooling
    stages:
      - stage: Dev
        jobs:
          - job: build_and_upload_dev
            displayName: Build and upload velopack to Dev environment
            templateContext:
              type: releaseJob
              isProduction: false
              inputs:
                - input: pipelineArtifact
                  pipeline: "apis-of-dotnet-build-planner"
                  artifactName: velopack-release
                  targetPath: $(Pipeline.Workspace)/velopack-release
            steps:
              - template: /pipelines/templates/upload-build-planner.yml@self
                parameters:
                  azureSubscriptionConnection: $(AzureSubscriptionConnectionDev)
                  storageAccountName: $(StorageAccountNameDev)
      - stage: Prod
        jobs:
          - job: PreDeploymentApprovalJob
            displayName: Pre-Deployment Approval
            condition: succeeded()
            timeoutInMinutes: 43200
            pool: server
            steps:
              - task: ManualValidation@1
                inputs:
                  notifyUsers: |-
                    Chris.Sfanos@microsoft.com,
                    v-danzhu1@microsoft.com
                  approvers: |-
                    Chris.Sfanos@microsoft.com,
                    v-danzhu1@microsoft.com
          - job: build_and_upload_velopack_prod
            displayName: Build and upload velopack to Prod environment
            templateContext:
              type: releaseJob
              isProduction: false
              inputs:
                - input: pipelineArtifact
                  pipeline: "apis-of-dotnet-build-planner"
                  artifactName: velopack-release
                  targetPath: $(Pipeline.Workspace)/velopack-release
            steps:
              - template: /pipelines/templates/upload-build-planner.yml@self
                parameters:
                  azureSubscriptionConnection: $(AzureSubscriptionConnectionProd)
                  storageAccountName: $(StorageAccountNameProd)